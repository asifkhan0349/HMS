$kind: http-request
name: List Rooms
method: GET
url: '{{baseUrl}}/api/rooms'
order: 1000
scripts:
  - type: afterResponse
    language: text/javascript
    code: |-
      const res = pm.response;
      const json = res.json();
      const SLA_MS = 2000;
      pm.test("[List Rooms] Status code is 200", () => { res.to.have.status(200); });
      pm.test("[List Rooms] Content-Type is application/json", () => { pm.expect(res.headers.get("Content-Type")).to.include("application/json"); });
      pm.test("[List Rooms] Response time is within SLA (" + SLA_MS + "ms)", () => { pm.expect(res.responseTime).to.be.below(SLA_MS); });
      pm.test("[List Rooms] Response is an array", () => { pm.expect(json).to.be.an("array"); });
      pm.test("[List Rooms] Each room has required fields", () => {
          if (json.length > 0) {
              const r = json[0];
              pm.expect(r).to.have.property("id");
              pm.expect(r).to.have.property("room_number");
              pm.expect(r).to.have.property("status");
              pm.expect(r.id).to.be.a("number");
          }
      });
      pm.test("[List Rooms] Array items have numeric ids", () => {
          json.forEach((item, idx) => { pm.expect(item.id, "item[" + idx + "].id").to.be.a("number"); });
      });
      if (json.length > 0) { pm.collectionVariables.set("lastResourceId", String(json[0].id)); }