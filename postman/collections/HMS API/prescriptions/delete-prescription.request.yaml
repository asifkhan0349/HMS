$kind: http-request
name: Delete Prescription
method: DELETE
url: '{{baseUrl}}/api/prescriptions/{{prescriptionId}}'
order: 5000
scripts:
  - type: afterResponse
    language: text/javascript
    code: |-
      const res = pm.response;
      const json = res.json();
      const SLA_MS = 2000;
      pm.test("[Delete Prescription] Status code is 200", () => { res.to.have.status(200); });
      pm.test("[Delete Prescription] Content-Type is application/json", () => { pm.expect(res.headers.get("Content-Type")).to.include("application/json"); });
      pm.test("[Delete Prescription] Response time is within SLA (" + SLA_MS + "ms)", () => { pm.expect(res.responseTime).to.be.below(SLA_MS); });
      pm.test("[Delete Prescription] Response body is a JSON object", () => { pm.expect(json).to.be.an("object"); });
      pm.test("[Delete Prescription] Response contains confirmation message or id", () => {
          const hasConfirmation = json.hasOwnProperty("message") || json.hasOwnProperty("detail") || json.hasOwnProperty("id");
          pm.expect(hasConfirmation, "Expected message, detail, or id in delete response").to.be.true;
      });
      pm.collectionVariables.unset("prescriptionId");