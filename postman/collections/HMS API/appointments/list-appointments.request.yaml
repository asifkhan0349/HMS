$kind: http-request
name: List Appointments
method: GET
url: '{{baseUrl}}/api/appointments'
order: 1000
scripts:
  - type: afterResponse
    language: text/javascript
    code: |-
      // â”€â”€â”€ Helpers â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€
      const res    = pm.response;
      const json   = res.json();
      const SLA_MS = 2000;
      
      // â”€â”€â”€ 1. Status & Protocol â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€
      pm.test("[List Appointments] Status code is 200", () => {
          res.to.have.status(200);
      });
      
      pm.test("[List Appointments] Content-Type is application/json", () => {
          pm.expect(res.headers.get("Content-Type")).to.include("application/json");
      });
      
      pm.test("[List Appointments] Response time is within SLA (" + SLA_MS + "ms)", () => {
          pm.expect(res.responseTime).to.be.below(SLA_MS);
      });
      
      // â”€â”€â”€ 2. Schema Assertions â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€
      pm.test("[List Appointments] Response is an array", () => {
          pm.expect(json).to.be.an("array");
      });
      
      pm.test("[List Appointments] Each appointment has required fields", () => {
          if (json.length > 0) {
              const appt = json[0];
              pm.expect(appt).to.have.property("id");
              pm.expect(appt).to.have.property("patient_id");
              pm.expect(appt).to.have.property("status");
              pm.expect(appt.id).to.be.a("number");
          }
      });
      
      pm.test("[List Appointments] Array items have numeric ids", () => {
          json.forEach((item, idx) => {
              pm.expect(item.id, "item[" + idx + "].id").to.be.a("number");
          });
      });
      
      // â”€â”€â”€ 3. Variable Chaining â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€
      if (json.length > 0) {
          pm.collectionVariables.set("lastResourceId", String(json[0].id));
      }